<div>
  <!-- Hero Section -->
  <section class="bg-white">
    <div class="container mx-auto px-6 py-4 text-center">
      <h1 class="text-5xl font-bold text-gray-800">Weather Report</h1>
      <% if (data) { %>
        <h3>Weather Data:</h3>
        <ul>
            <li><%= data.city %>: <%= data.temperature %>°C - <%= data.description %></li>
        </ul>
      <% } else { %>
        <p>Could not fetch weather data.</p>
      <% } %>
    </div>
  </section>

  <!-- Features Section -->
  <!-- <section class="bg-gray-100">
    <div class="container mx-auto px-6 py-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-800">Objective</h2>
        <p class="mt-4 text-gray-600">Testing Azure AD</p>
      </div>
      <div class="flex flex-wrap justify-center">
        <div class="w-full md:w-1/3 px-4 py-4">
          <div class="bg-white p-6 rounded-lg shadow-lg">
            <h3 class="text-2xl font-bold text-gray-800 mb-3">Oauth</h3>
            <p class="text-gray-600">It demonstrates how to authentication using OAuth protocol.</p>
          </div>
        </div>
      </div>
    </div>
  </section> -->
</div>
