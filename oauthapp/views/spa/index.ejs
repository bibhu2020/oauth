<div>
    <!-- Hero Section -->
    <section class="bg-white">
      <div class="container mx-auto px-6 py-1">
        <h2 class="text-5xl font-bold text-gray-800  text-center">Read Users from Microsoft Graph (SPA) 
        </h2>
        <div class="w-full mt-1" id="app">
          <img src="/images/loading.gif" v-if="loading" class="mx-auto" />
        </div>
      </div>
    </section>
</div>

<script>
  // Pass the tenantId from the server-side to the client-side JavaScript
  //alert('<%= data.apiUrl %>');
  window.apiUrl = '<%= data.apiUrl %>';
  window.apiClientId = '<%= data.apiClientId %>';
  window.clientId = '<%= data.clientId %>';
  window.tenantId = '<%= data.tenantId %>';
  window.redirectUrl = '<%= data.redirectUrl %>';
</script>

<!--This is vuejs scripts that is generated by webpack during the build-->
<script src="/js/bundle.js"></script>
